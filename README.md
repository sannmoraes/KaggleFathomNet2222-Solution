# FathomNet-OOD-Detection
 Deep Learning project developed in Python to recognize handwritten digits (0–9) using a **Convolutional Neural Network (CNN)** built with TensorFlow/Keras.  The project performs the complete machine learning workflow, including data loading, preprocessing, exploratory analysis, model training, validation, and prediction generation.
